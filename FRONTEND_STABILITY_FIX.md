# 前端稳定性修复说明

本版本修复了新闻前端改动后导致主图表、数据表和关键观察指标不渲染的问题。

核心改动：
- 页面加载时先用内置备用数据渲染，避免 JSON 读取失败导致空白；
- `news.json` 和 `candidates.json` 读取失败时，只影响对应板块，不影响主图表；
- `renderAll()` 中各板块独立 try/catch，一个板块报错不会拖垮整个 dashboard；
- “刷新新闻数据”只重新读取本站 `data/news.json`，不直接访问 GDELT。
